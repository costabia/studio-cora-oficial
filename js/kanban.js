import {KANBAN} from './data.js';
import {esc} from './utils.js';
export function kanban(client,editable=true){return `<div class="kanban">${KANBAN.map(status=>{const items=client.tasks.filter(t=>t.status===status);return `<section class="kanban-col" data-status="${status}"><div class="col-head"><strong>${status}</strong><span class="count">${items.length}</span></div>${items.map(t=>`<article class="task-card" draggable="${editable}" data-task="${t.id}" data-open-task="${t.id}"><h4>${esc(t.title)}</h4><p>${esc(t.currentDescription||t.desc||'Nenhuma descrição adicionada.')}</p><div class="task-foot"><span>Clique para ver detalhes</span></div></article>`).join('')||'<div class="empty" style="padding:20px 5px;font-size:11px">Nenhum item</div>'}</section>`}).join('')}</div>`}
export function bindDrag(root,onDrop){root.querySelectorAll('[draggable=true]').forEach(card=>{card.addEventListener('dragstart',e=>e.dataTransfer.setData('text/plain',card.dataset.task))});root.querySelectorAll('.kanban-col').forEach(col=>{col.addEventListener('dragover',e=>e.preventDefault());col.addEventListener('drop',e=>{e.preventDefault();onDrop(e.dataTransfer.getData('text/plain'),col.dataset.status)})})}
